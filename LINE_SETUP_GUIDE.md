# 📋 คู่มือการตั้งค่าระบบ LINE Bot & LINE Login (สำหรับเจ้าของหอพัก)

คู่มือนี้จะช่วยแนะนำขั้นตอนการเปลี่ยนมาใช้บัญชี **LINE Developers** ของเจ้าของหอพักเอง เพื่อให้ระบบส่งบิลและแจ้งเตือนผ่านบอทที่เป็นชื่อของหอพักคุณโดยเฉพาะ

---

## 🌟 ภาพรวมระบบ LINE ที่ใช้งาน
ระบบหอพักสมบัติใช้งานบริการของ LINE 2 ส่วนหลักๆ ดังนี้:
1. **LINE Messaging API (LINE Bot)**: สำหรับส่งบิลตรงหาผู้เช่า และแจ้งเตือนแอดมินเวลามีคนโอนเงิน
2. **LINE Login**: สำหรับให้ผู้เช่ากดล็อกอินเพื่อเชื่อมโยงห้องพักกับไลน์ส่วนตัว (Account Linking)

---

## 🛠️ ขั้นตอนที่ 1: ลงทะเบียนบัญชีนักพัฒนา (LINE Developers)
1. เข้าไปที่เว็บไซต์ [LINE Developers Console](https://developers.line.biz/)
2. เข้าสู่ระบบด้วยบัญชี LINE ส่วนตัว หรือ LINE Business ของคุณ
3. สร้าง **Provider** (เช่น ตั้งชื่อว่า `Sombat Apartment` หรือชื่อหอพักของคุณ)

---

## 🤖 ขั้นตอนที่ 2: ตั้งค่าระบบ LINE Bot (Messaging API)
ใช้สำหรับส่งบิลและส่งยอดชำระเงินหาผู้เช่า

### 1. สร้าง Channel
1. ในหน้า Provider ที่สร้างไว้ กดปุ่ม **Create a new channel**
2. เลือกประเภทเป็น **Messaging API**
3. กรอกข้อมูลบอทของคุณ:
   * **ชื่อบอท (Channel name)**: (เช่น บอทหอพักสมบัติ)
   * **รูปโปรไฟล์**: อัปโหลดรูปภาพโลโก้หอพักของคุณ
   * **ประเภทธุรกิจ**: เลือกอสังหาริมทรัพย์ / บริการอื่นๆ
4. กดบันทึกและยอมรับเงื่อนไข

### 2. นำข้อมูลไปกรอกในระบบแอดมิน
1. ไปที่แท็บ **Messaging API** ของบอทที่เพิ่งสร้าง
2. เลื่อนลงไปล่างสุดตรงหัวข้อ **Channel access token** กดปุ่ม **Issue** เพื่อสร้างโทเค็น
3. คัดลอกโทเค็นยาวๆ นั้น มาใส่ในหน้า **"ตั้งค่าเซิร์ฟเวอร์ & Supabase"** ของระบบแอดมิน ในช่อง **LINE Channel Access Token**
4. ในหน้าเดียวกัน ให้ใส่ **LINE User ID หรือ Group ID แอดมิน** ที่ต้องการให้แจ้งเตือนยอดโอนเงิน (คุณสามารถแชทคุยกับบอทแล้วพิมพ์คำว่า `"ไอดี"` หรือ `"เช็คไอดี"` บอทจะตอบกลับเลขไอดีของคุณเพื่อนำมาใส่ในช่องนี้ครับ)
5. กดปุ่มสีเขียว **"บันทึก LINE Settings"**

### 3. ตั้งค่า Webhook เพื่อรับข้อความสลิป
เพื่อให้บอทสามารถตรวจจับการพิมพ์เลขห้อง หรือรูปภาพสลิปของผู้เช่าได้:
1. ในแท็บ **Messaging API** ค้นหาหัวข้อ **Webhook URL**
2. กด Edit และระบุค่า URL ดังนี้ (เปลี่ยน `bdeowpdjgiombqatdilh` เป็น Project Ref ของคุณหากมีการเปลี่ยนโปรเจกต์):
   ```text
   https://bdeowpdjgiombqatdilh.supabase.co/functions/v1/line-notify
   ```
3. กดบันทึกแล้วเปิดสวิตช์หัวข้อ **Use webhook** ให้เป็นสีเขียว
4. เลื่อนลงมาที่หัวข้อ **LINE Official Account features** ➔ กดแก้ไขตรง **Auto-reply messages** ➔ ตั้งค่าปิด (Disabled) การตอบกลับอัตโนมัติของ LINE และปิดข้อความต้อนรับของ LINE เพื่อให้ระบบตอบกลับอัตโนมัติจากโค้ดของหอพักทำงานได้อย่างสมบูรณ์

---

## 🔑 ขั้นตอนที่ 3: ตั้งค่าระบบล็อกอินผู้เช่า (LINE Login)
ใช้สำหรับให้ผู้เช่าล็อกอินเพื่อผูกสิทธิ์บัญชีไลน์กับเลขห้องพัก

### 1. สร้าง Channel
1. ในหน้า Provider เดิม กดปุ่ม **Create a new channel** อีกครั้ง
2. เลือกประเภทเป็น **LINE Login**
3. ตั้งชื่อ Channel name (เช่น ล็อกอินผู้เช่าหอพัก)
4. ตรงหัวข้อ **App types** ให้เลือกติ๊กถูกที่ช่อง **Web app**
5. กดบันทึก

### 2. ตั้งค่า Redirect URI (Callback)
เพื่อให้เมื่อผู้เช่าล็อกอินสำเร็จแล้ว ระบบจะส่งกลับมาบันทึกข้อมูลในฐานข้อมูล:
1. ไปที่แท็บ **LINE login settings**
2. ตรงหัวข้อ **Callback URL** ให้กด Edit แล้วกรอก URL นี้ลงไป:
   ```text
   https://bdeowpdjgiombqatdilh.supabase.co/functions/v1/line-login-callback
   ```
3. กดบันทึก

### 3. ตั้งค่า Channel ให้เป็นสาธารณะ (Active)
* ด้านบนสุดของหน้าตั้งค่า จะมีคำว่า **Developing** ให้กดเปลี่ยนสถานะเป็น **Published** เพื่อเปิดให้บุคคลทั่วไป (ผู้เช่า) สามารถกดล็อกอินได้

---

## 💾 ขั้นตอนที่ 4: อัปเดต Secret ใน Supabase
เพื่อความปลอดภัยสูง คีย์สำหรับหน้าล็อกอินจะถูกบันทึกไว้ในระบบความลับหลังบ้านของ Supabase:

1. เข้าสู่ระบบ [Supabase Dashboard](https://supabase.com/)
2. เลือกโปรเจกต์หอพักของคุณ (`Sombat Apartment`)
3. ไปที่เมนู **Settings** (รูปเกียร์ซ้ายล่าง) ➔ **Edge Functions**
4. ในส่วนของ **Manage secrets** ให้กดปุ่ม **Add new secret** เพื่อเพิ่มคีย์ดังนี้:
   * ➕ **ชื่อคีย์**: `LINE_LOGIN_CHANNEL_ID`
     * **ค่าข้อมูล**: ดูได้จากแท็บ *Basic settings* ในหน้า *LINE Login Channel* ที่คุณสร้างในขั้นตอนที่ 3 (เป็นตัวเลข)
   * ➕ **ชื่อคีย์**: `LINE_LOGIN_CHANNEL_SECRET`
     * **ค่าข้อมูล**: ดูได้จากแท็บ *Basic settings* ในหน้า *LINE Login Channel* ของคุณ
   * ➕ **ชื่อคีย์**: `LINE_REDIRECT_URI` (ถ้ามี)
     * **ค่าข้อมูล**: กรอก `https://bdeowpdjgiombqatdilh.supabase.co/functions/v1/line-login-callback`
5. กดบันทึกเรียบร้อย

---

✨ **เสร็จสิ้นการตั้งค่า!** ตอนนี้ระบบแจ้งหนี้และระบบยืนยันสลิปของหอพักจะเปลี่ยนไปทำงานผ่าน LINE บัญชีใหม่ของหอพักคุณอย่างสมบูรณ์แบบร้อยเปอร์เซ็นต์ครับ!
