# สรุป: แยกฐานข้อมูลเป็นคอลัมน์ตามประเภท (เหมือนชีตแยกแท็บ)

## ก่อนหน้านี้เป็นยังไง
ทุกอย่าง (ห้อง/ผู้เช่า/บิล/บัญชี/ซ่อม/เอกสาร) ถูกยัดเป็น **JSON ก้อนเดียว** ในแถวเดียวของตาราง
`apartment_state` ทุกครั้งที่บันทึกคือ**เขียนทับทั้งก้อน** — ถ้าสองเครื่องบันทึกพร้อมกัน ฝ่ายที่บันทึกทีหลัง
จะเขียนทับข้อมูลของอีกฝ่ายทั้งหมด และไม่มีอะไรกันบิลซ้ำในระดับฐานข้อมูลจริง

## ตอนนี้เป็นยังไง
แยกเป็น **13 ตาราง** ตามประเภทข้อมูล คอลัมน์ชัดเจนทุกตาราง:

| ตาราง | ข้อมูล |
|---|---|
| `settings` | ตั้งค่าระบบ (ชื่อหอพัก, ที่อยู่, บัญชีธนาคาร ฯลฯ) |
| `rates` | อัตราค่าน้ำ/ไฟ/ค่าส่วนกลาง |
| `users` | ผู้ใช้งานระบบ |
| `room_types` | ประเภทห้องเช่า |
| `rooms` | ข้อมูลห้องเช่า |
| `tenants` | ผู้เช่า + วันเริ่ม/สิ้นสุดสัญญาเช่า + เงินมัดจำ |
| `tenant_documents` | เอกสารผู้เช่า (สำเนาบัตร ปชช. / ทะเบียนบ้าน / อื่นๆ) |
| `tenant_deposit_deductions` | รายการหักเงินมัดจำ |
| `invoices` | ใบแจ้งหนี้/บิล — **มี UNIQUE(room_id, month_key) กันบิลชนกัน** |
| `repairs` | แจ้งซ่อม |
| `ledger` | บัญชีรายรับ-รายจ่าย |
| `events` | ปฏิทิน/นัดหมาย |

การบันทึกตอนนี้จะ**ส่งเฉพาะแถวที่เปลี่ยนจริง**ไปยังตารางที่เกี่ยวข้องเท่านั้น (ไม่ทับทั้งก้อนอีกต่อไป)
ทำให้แก้ห้อง แก้ผู้เช่า ออกบิล พร้อมกันจากหลายเครื่องได้โดยไม่ชนกัน และบิลห้องเดียวกัน+เดือนเดียวกัน
จะถูกกันซ้ำจริงในระดับฐานข้อมูล (ไม่ใช่แค่กรองซ้ำฝั่งหน้าเว็บเหมือนเดิม)

## ขั้นตอนติดตั้ง (ทำครั้งเดียว)

1. เข้า Supabase Dashboard ของโปรเจกต์ → **SQL Editor** → New query
2. รันไฟล์ **`1_schema.sql`** ทั้งไฟล์ก่อน (สร้างตาราง + RLS + RPC สำหรับพอร์ทัลผู้เช่า)
3. รันไฟล์ **`2_migrate_from_apartment_state.sql`** ต่อ (ย้ายข้อมูลเดิมจาก `apartment_state`
   เข้าตารางใหม่ทั้งหมด — รันซ้ำได้ไม่ทำข้อมูลซ้ำ)
4. เปิดแอป (index.html) → หน้าตั้งค่า → กด **"ซิงค์ข้อมูลลง Supabase ตอนนี้"** เพื่อดึงข้อมูลจากตารางใหม่
   เข้ามาแสดงในแอป แล้วลองใช้งานทุกเมนู (ห้อง, ผู้เช่า, ออกบิล, ซ่อม, บัญชี) ให้ครบ
5. เมื่อมั่นใจว่าข้อมูลถูกต้องครบถ้วนแล้ว (ลองสัก 2-3 วัน) ค่อยลบตารางเก่า:
   ```sql
   drop table public.apartment_state;
   ```

## พอร์ทัลผู้เช่า (tenant.html)
ไม่ต้องแก้โค้ดฝั่งพอร์ทัลผู้เช่าเลย เพราะเดิมเรียกผ่าน RPC (`get_room_list`, `get_tenant_bill`,
`submit_tenant_payment`, `submit_tenant_repair`) อยู่แล้ว — ไฟล์ `1_schema.sql` สร้างฟังก์ชันเหล่านี้ใหม่
ให้ดึงจากตารางแยกประเภทแทน โดยหน้าตาผลลัพธ์ (JSON) เหมือนเดิมทุกประการ

## หมายเหตุเรื่องรูปภาพ/สลิป/เอกสาร
คอลัมน์ `file_url` ใน `tenant_documents` และ `slip_url` ใน `invoices` รองรับทั้ง URL จาก
Supabase Storage (แนะนำ ใช้พื้นที่ฐานข้อมูลน้อยกว่า) และ data URL แบบ base64 ที่แอปฝั่งแอดมินยังใช้อยู่
ถ้าต้องการให้แอดมินอัปโหลดไฟล์ขึ้น Storage จริงเหมือนฝั่งผู้เช่า (ที่อัปโหลดสลิปขึ้น Storage อยู่แล้ว)
แจ้งมาได้ จะแก้ส่วนฟอร์มเพิ่มเอกสารผู้เช่าให้อัปโหลดขึ้น Storage แทนการฝัง base64
